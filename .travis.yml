language: generic

sudo: required

#env:
#  - test: Ubuntu 14.04 Apache
#    distribution: ubuntu
#    version: 14.04
#    init: /sbin/init
#    run_opts: ""

addons:
  hosts:
    - aegir.local.computer
    - sitetest.aegir.local.computer

services:
  - docker

before_script:

  # Generate SSH key so it doesn't ask us for a password.
  - sudo ssh-keygen -t rsa -N "" -f /root/.ssh/id_rsa
  - sudo su -c "echo 'Host *' >> ~/.ssh/config; echo '  StrictHostKeyChecking no' >> ~/.ssh/config "
  - sudo chmod 400 ~/.ssh/config
  - sudo cat ~/.ssh/config
  - sudo ls -la ~/.ssh
  - sudo apt-get install php5-cli
  - sudo wget https://github.com/drush-ops/drush/releases/download/8.1.3/drush.phar
  - sudo mv drush.phar /usr/local/bin/drush
  - sudo chmod +x /usr/local/bin/drush

script:
  - sudo bash prepare.sh